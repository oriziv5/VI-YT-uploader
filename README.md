# Video Indexer Uploader Tool

Fetch batch links to Video Indexer

## Prerequisites: Node.js v14+

`npm install`

to run the ui in dev mode : `npm run dev-ui`

run the server in dev mode:

`npm run dev`

